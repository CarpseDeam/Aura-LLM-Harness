Cleanup. We've now hit `client._base_url` and `client._default_model` from external consumers twice. Just expose the OllamaClient construction params properly so this stops coming up.

Changes:

1. In `src/aura_harness/llm/ollama_client.py`, add three new read-only properties next to the existing `default_model` property:
   ```python
   @property
   def base_url(self) -> str:
       """Root URL of the Ollama server (no trailing slash)."""
       return self._base_url

   @property
   def timeout_seconds(self) -> float:
       """Per-request timeout for non-health requests, in seconds."""
       return self._timeout

   @property
   def log_path(self) -> Path:
       """Path to the JSONL call log."""
       return self._log_path
   ```

2. In `src/aura_harness/ui/main_window.py`, replace `self._client._base_url` with `self._client.base_url` (and remove any `noqa: SLF001` on that line if present).

3. Don't change behavior. No new tests required for the properties themselves — they're trivial getters. Existing tests continue to cover behavior.

4. Run the full test suite to confirm nothing regressed.

Definition of done:
- All four construction params (`base_url`, `default_model`, `timeout_seconds`, `log_path`) are exposed as read-only properties on OllamaClient
- `main_window.py` uses `client.base_url` instead of `client._base_url`
- All 32 tests still pass
- No TODOs added

Be methodical. Read what you need, make the change, stop. Match existing code style. Type hints everywhere.