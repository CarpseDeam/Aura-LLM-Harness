Quick cleanup. The lab module reads `client._default_model` with a `noqa: SLF001` because the constructor param is stored as a private attribute. Fix this properly.

Changes:

1. In `src/aura_harness/llm/ollama_client.py`, add a public read-only property:
   ```python
   @property
   def default_model(self) -> str:
       """Model name used when ``generate`` is called without an explicit model."""
       return self._default_model
   ```
   Place it after `__init__` and before `generate`.

2. In `src/aura_harness/lab/generator.py`, replace the `client._default_model` access (and the `noqa: SLF001` comment if present on that line) with `client.default_model`.

3. Don't expose any of the other underscore-prefixed attributes (`_base_url`, `_timeout`, `_log_path`). Only `default_model` is needed externally for now.

4. No new tests required — existing tests already cover the behavior. Just confirm the full test suite still passes after the change.

Definition of done:
- `client.default_model` is a public read-only property
- `lab/generator.py` no longer accesses `_default_model` and no longer carries that `noqa`
- All 24 tests still pass
- No TODOs added

Be methodical. Read what you need, make the change, stop. Match existing code style. Type hints everywhere.