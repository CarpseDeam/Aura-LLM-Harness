## Spec: Sharpen `bench/tasks/duplicate_finder/spec.md`

The current spec says the right things but buries the two requirements that the 7B coder model keeps missing: function name/location at module level, and relative-forward-slash paths. Failure data: 16/24 candidates failed on path format (absolute Windows paths or backslashes), 7/24 failed on function name/scope (NameError on import or "no callable 'find_duplicates'"). The spec content is correct — we just need to surface those two contract points where the model can't ignore them.

**Change to `bench/tasks/duplicate_finder/spec.md` only.** Do not modify verify.py, fixtures, expected_report.json, or any other file.

Add a new section titled "Critical contract requirements" immediately after the opening paragraph (before the existing "Function signature" section). It should be three blunt bullets covering:

1. The function must be named exactly `find_duplicates` and defined at module top level. Not nested inside a class, not nested inside `if __name__ == "__main__":`. The verifier imports the module and calls `module.find_duplicates(...)` directly — anything else fails.

2. Paths in the JSON output must be **relative to the `directory` argument**, not absolute. Use `pathlib.Path(p).relative_to(directory)` or equivalent.

3. Paths must use forward slashes on every platform, including Windows. Use `.as_posix()` on the relative `Path` — do not use `str(path)` because that emits backslashes on Windows.

Then add a brief "Minimal example" section right after the existing "Report shape" section, showing the canonical path-conversion pattern in roughly 5-8 lines:

```python
from pathlib import Path

base = Path(directory)
for absolute_file_path in matched_files:
    relative = absolute_file_path.relative_to(base).as_posix()
    # 'relative' is what goes into the JSON
```

Keep the existing content intact otherwise. The goal is to make the two contract requirements impossible to miss — short, blunt, with a code pattern that resolves both at once.

**Definition of done:** spec.md has the new "Critical contract requirements" section near the top and the "Minimal example" snippet. No other files touched.

Be methodical. Read what you need, make the change, stop. Match existing code style. Type hints everywhere.