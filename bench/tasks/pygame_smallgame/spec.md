Build a small pygame game runnable with `python main.py`. Single window, keyboard input, some kind of win/lose state. Pick the genre yourself — anything that fits in a few hundred lines. The code should be clean enough to commit: clear module boundaries, no dead code, a brief README in the same directory explaining how to run it. Constraints: pygame and python-stdlib only, no other dependencies.
